import { atom } from 'jotai';

export const NAME_ATOM = atom<string | null | undefined>('');
export const IMAGE_ATOM = atom<string | null | undefined>('');
export const EMAIL_ATOM = atom<string | null | undefined>('');
